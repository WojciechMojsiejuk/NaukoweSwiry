#include <stdio.h>
#include <math.h>

int main()
{
    int n=20, i, k;
    printf("Program ma z³ozonosc obliczneniowa 2^%d", n);
    for (i=0;i<pow(2, n);i++)
    {
        k=2*3;
    }
    return 0;
}
